export interface TimetablePrettyEntry {
    subject: string
    count: number
    duration: number
    days: string[]
    classes: string[]
    groups: string[]
    teachers: string[]
    startPeriod: string
}