/** The ID string for a timetable week definition */
export declare type TimetableWeekDefinitionId = string

export interface TimetableWeekDefinition {
    id: TimetableWeekDefinitionId
    vals: [string]
}